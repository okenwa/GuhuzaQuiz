import QuizLevelCard from "./quizLevelCard";
import React from "react";
import { Lavels } from "@/lib/db";
type QuizLevelSectionType = { 
    quantity : number; 
    
}
function QuizLevelSection({ quantity }: QuizLevelSectionType) {
  return {Lavels};
}

export default QuizLevelSection;
